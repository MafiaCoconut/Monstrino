import pytest

@pytest.mark.asyncio
async def test_set_default_values(
        seed_source_list,
        seed_character_role_list,
        seed_release_type_list,
        seed_relation_type_list,
        seed_exclusive_vendor_list,
        seed_image_reference_origin_list,
):
    pass